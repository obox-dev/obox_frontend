import { Button } from 'react-bootstrap';

const LoginButton = () => {
  return (
    <Button variant="dark text-light border-light" className="m-3">
      Log In
    </Button>
  );
};

export default LoginButton;
